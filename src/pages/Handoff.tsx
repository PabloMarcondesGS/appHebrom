import React from 'react';
import { View, Image } from 'react-native';

// import { Container } from './styles';

export const Handoff: React.FC = () => {
  return (
      <View style={{ flex: 1 }}>
          <Image source={require('../assets/Raster.png')} style={{}} />
      </View>
  );
}
